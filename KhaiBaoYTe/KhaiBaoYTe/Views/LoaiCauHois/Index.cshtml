@model IEnumerable<KhaiBaoYTe.Models.LoaiCauHoi>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DangCauHoi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DangCauHoi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDLoaiCauHoi }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDLoaiCauHoi }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDLoaiCauHoi })
        </td>
    </tr>
}

</table>
