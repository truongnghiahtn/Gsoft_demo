@model KhaiBaoYTe.Models.Sub_CauHoi

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="text-center">
    <h2>Delete</h2>
    <h4>Nội dung</h4>
    <h5 class="text-danger">Are you sure you want to delete this?</h5>
</div>
<hr />
<div>
    <div class="row justify-content-md-around">
        <div class="col-md-7">
            <dl class="row">
                <dt class="col-6">
                    Tiêu đề
                </dt>
                <dd class="col-6">
                    @Html.DisplayFor(model => model.CauHoi.TieuDe)
                </dd>
                <dt class="col-6">
                    Nội dung
                </dt>
                <dd class="col-6">
                    @Html.DisplayFor(model => model.NoiDung)
                </dd>
            </dl>
        </div>
    </div>
    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color text-center mt-md-4">
            <input type="submit" value="Delete" class="btn btn-outline-danger" /> 
            <a href="@Url.Action("Index", "SubCauHoi", new { stringId = Request.QueryString["stringId"] })" class="btn btn-default">
                Back to Nội dung
            </a>
        </div>
    }
</div>
